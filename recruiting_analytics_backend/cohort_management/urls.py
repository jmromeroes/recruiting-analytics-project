from django.urls import path
"""path(
        "cohorts/",
        CohortManagementAPI.as_view(),
        name="cohort_management",
    ),
    path(
        "cohort/<cohort_identifier>/people/",
        CohortPeopleAPI.as_view(),
        name="people_management",
    ),
    path(
        "cohort/<cohort_identifier>/send_reminder",
        SendReminderAPI.as_view(),
        name="people_send_reminder",
    ),
    path(
        "public/cohort/<cohort_slug>",
        CohortPublicAPI.as_view(),
        name="cohort_public",
    ),
    path(
        "notification_handler/section_completed",
        HandleSectionCompletedNotification.as_view(),
        name="sns_handler_section_completed",
    ),
    path(
        "notification_handler/learning_started",
        HandleLearningStartedNotification.as_view(),
        name="sns_handler_learning_started",
    ),"""
urlpatterns = [
    
]
